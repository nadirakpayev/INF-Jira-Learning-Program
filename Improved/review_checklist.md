# Чеклист ревьюера

Отмечайте каждый пункт как `Да / Нет / Частично / N/A`.

## 1. Общие условия

- Кандидат сдавал материалы из `Improved`, а не произвольный набор артефактов.
- `Source Docs` не использовались как скрытый источник требований.
- Пакет сдачи соответствует `submission_format.md`.
- Проверка возможна без длинной устной презентации.

## 2. Блок 1

- Подтвержден собственный `Atlassian Cloud` site.
- Есть один `company-managed service project`.
- Созданы минимум `3` `Components`.
- `Components` используются как системы, а не просто для количества.
- Есть каталог с обязательным минимумом услуг.
- Есть request type `Incident`.
- Есть request type `Service Request`.
- Есть маршрутизация через `Components` и default assignee или честно описанный platform gap.
- Процесс `Incident` рабочий.
- Процесс `Service Request` рабочий.
- Для обоих процессов есть `SLA`.
- Для обоих процессов есть `Queues`.

## 3. Блок 2

- Использован выданный `CMDB`-датасет.
- Есть модель сущностей и связей.
- Есть локальный `PostgreSQL`.
- Развертывание выполнено без контейнеров.
- Есть evidence из `pgAdmin`.
- Есть таблицы с `PK`.
- Есть связи с `FK`.
- Есть test / seed data.
- Есть проверочные SQL-запросы.
- Есть связка `Major Incident -> Problem -> Change`.
- Показано, какие данные живут в `Jira`, а какие во внешней модели.
- Есть `SLA` и `Queues` для второго блока.

## 4. Screening decision

- `Блок 1` сдан.
- `Блок 2` сдан.
- Нет критических ошибок.
- Итоговый балл не ниже `65`.

## 5. Блок 3 и Блок 4

Проверяется только после `PASS`.

- `Блок 3` завершен или находится в работе.
- `Блок 4` завершен или находится в работе.
- Кандидат переносит уже освоенные паттерны на новые процессы.

## Итог ревьюера

- `PASS`
- `FAIL`

## Краткий комментарий

Зафиксируйте:
- 3 сильные стороны;
- 3 слабые стороны;
- нужен ли короткий follow-up;
- допущен ли кандидат к `Блоку 3` и `Блоку 4`.
